// create an employee
class Employee{
    // add constructor for name, id and email
    constructor(name, id, email) {
        // assign id, name and email
        this.id = id;
        this.email = email;
        this.name = name;
        // assign role
        this.role = "Employee";
    }
}